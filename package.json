{
  "name": "whitebox-pro-mail-postmark",
  "version": "0.1.0",
  "description": "Postmark provider for whitebox-pro-server-plugin-mail — send (Postmark SDK), inbound + tracking webhook parsing, Basic-auth webhook verification.",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/almero-digital-marketing/whitebox-pro-mail-postmark.git"
  },
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "test": "vitest run"
  },
  "peerDependencies": {
    "whitebox-pro-server-plugin-mail": "^0.1.0"
  },
  "dependencies": {
    "postmark": "^4.0.5"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  },
  "files": [
    "src",
    "README.md"
  ]
}
